# NOTICE

## 1. 算法来源

本项目的 NCM 容器解密流程，是对**公开在线工具** `www.ncm2mp3.com` 客户端 JavaScript
（`/js/decrypt.js`）所做的**互操作性还原**：该项目自行提取该站点的 webpack 模块并逐字节对拍验证，
再由纯 Python 重写。同时参考了以下已公开的开源实现：

- `taurusxin/ncmdump`
- `unlock-music`
- `Johnserf-Seed/ncm2mp3`
- `nowa277/OpenConverter`

本仓库**不包含**上述任何项目的源代码，也未复制其实现；只包含独立编写的 Python 代码，
以及 NCM 容器格式本身的固定常量。

## 2. 关于源码中的两个十六进制常量

`ncmdump/ncm_core.py` 中的 `CORE_KEY` 与 `META_KEY` 是 **NCM 容器格式的固定算法常量**：

- 它们**不是**账号凭据、令牌或任何用户秘密；不随用户变化、无需登录、与服务端鉴权无关；
- 它们属于容器格式规格的一部分，已在上述多个公开项目中广泛出现；
- 本仓库未因此暴露任何额外的、非公开的信息。

也就是说，从**信息保密**角度看，这两个常量不构成泄密。

> ⚠️ 但这**不等于没有法律风险**。"常量是公开的"只回答了"泄密"这一个问题；是否构成
> "绕过技术保护措施"是**另一条独立的判断轴**（例如美国 DMCA §1201、各平台服务条款），
> 与常量是否公开无关。使用者需自行评估并承担相应责任。

我们刻意**不做**字符串拼接或异或混淆来"藏"这些常量 —— 那只是隐藏而非移除，
反而会给人刻意规避的观感。

## 3. 授权范围与使用限制

本项目以 [MIT](LICENSE) 发布，仅提供**格式转换**能力：

- 全程**本地运行**，不联网、不上传、不代理账号，不含任何绕过登录或付费校验的功能；
- **不包含**任何音频、歌词、专辑封面等受版权保护的素材（仓库内无一个 `.ncm`/`.flac`/`.mp3`/
  `.lrc`/图片文件）；
- 不做任何音乐内容的下载、分发或再发布。

请仅用于转换**你自己合法获得**的文件，并遵守你所在地区的法律与相关平台的服务条款。
因使用本项目产生的一切后果由使用者自行承担。

## 4. 无担保

本软件按"现状"提供，不附带任何明示或暗示的担保。作者不对数据丢失、文件损坏或
任何间接损失负责。**转换前请自行保留原始文件的备份** —— 命令行默认会在校验通过后删除源
`.ncm`（可用 `--keep` 关闭）。
