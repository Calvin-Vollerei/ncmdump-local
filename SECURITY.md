# Security Policy

## 报告问题

请通过 GitHub 的 **Private vulnerability reporting**（仓库 → Security → Report a vulnerability）
私下报告，不要直接开公开 issue。也可以在 issue 里只写"安全问题，请私聊"，不写细节。

我会在 7 天内回应。请说明：影响范围、复现步骤、以及你建议的修复方向。

## 这个项目处理什么数据

* **只处理本地文件**：读取用户指定的 `.ncm`，写出音频文件。不联网（除可选的一次性封面下载）。
* **不涉及账号**：没有登录、没有令牌、不接触任何服务端接口。
* **不收集遥测**：不上报任何使用数据。

## 已知的、非漏洞的行为

| 行为 | 说明 |
| --- | --- |
| 源码内含两个十六进制常量 | NCM 容器格式的固定常量，非凭据；见 [NOTICE.md](NOTICE.md) |
| 默认会在校验通过后删除源 `.ncm` | 刻意的行为，用 `--keep` 关闭；校验不通过则保留 |
| 可选的封面下载会访问图片 CDN | 仅在勾选/未加 `--no-cover` 时发生，只发一次 GET |
| 启动日志写在用户日志目录 | 内容已做路径脱敏，且**不会**写进项目目录 |

## 供应链与发布卫生

* 核心包**零第三方运行时依赖**（仅标准库），CI 中有守卫任务验证这一点
* 可选的 `numpy` / `PySide6` / `pywinstyles` 只在显式安装对应 extra 时引入
* 仓库禁止提交：音频/歌词/封面素材、`.ncm` 等加密容器、打包产物、
  日志与字节码缓存、含绝对路径的文件、任何凭据
* CI 每次提交都会扫描上述内容（见 `.github/workflows/ci.yml`）

## 支持范围

只对最新 `main` 分支的安全问题做修复。
